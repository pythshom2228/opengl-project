#include "../include/Collider.h"


Collider::Collider(const std::array<std::int32_t,2> scale) : _scale(scale) {}

void Collider::update(const glm::vec3 verticesPos) {
   
}


bool Collider::checkIfCollides(const Collider& other) const {
    
}
  
